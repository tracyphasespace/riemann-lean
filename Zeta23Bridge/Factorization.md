# The Factorization Experiment: does [Z23] factor through `EulerStiffness`?

**Status: arithmetic factorization complete (F0–F4a); full abstract theorem pending coefficient-free assembly (F4-final). This document is the running record of the ledger's production commitment (ii) (`Papers/Critical_Line_Ledger_Aug2026.md`, §8): *either prove that [Z23]'s bound factors through `EulerStiffness`, or report the obstruction to doing so.* Started and brought to arithmetic completeness August 12, 2026; revision history in git. Current formal state (pasted from `audit.sh` output): `# SUMMARY: 81 theorems, 81 audited, 0 gaps, 0 sorryAx` (three of the 81 — `Sponge.embed_add`, `embed_injective`, `mem_stratum_iff` — use no axioms at all); audit enforced by `audit.sh` (declared-vs-audited diff with count assertion, fails on gap or mismatch) and by CI (`.github/workflows/audit.yml`).**

---

## F0. The paper-level dependency audit

Method: every lemma/proposition of [Z23] §§2–6 was checked for the arithmetic it consumes, and each input classified as **FIELD** (one of `EulerStiffness`'s five fields), **DERIVED** (provable from the fields alone), or **PACKAGE** (a property of the completed function/zero side, not of the coefficients — outside the interface by design).

| [Z23] item | Arithmetic consumed | Classification |
|---|---|---|
| Lemma 5.1, Σ_{n≤x} Λ(n) ≪ x | Chebyshev bound | **FIELD** (`chebyshev`) |
| Lemma 5.1, Σ Λ(n)²/n = ½log²x + O(log x) | Mertens second moment | **FIELD** (`mertens_energy`) |
| Lemma 5.1, Σ Λ(n)² ≪ x·log x | — | **DERIVED** — `sum_sq_le` |
| Lemma 5.1, Σ Λ(n)/√n ≪ √x (and its log-weighted variant) | — | **DERIVED** — `sum_div_sqrt_le` (sharp, dyadic descent) and `sum_mul_log_div_sqrt_le`; crude Cauchy–Schwarz grade `sum_div_sqrt_sq_le` retained |
| (5.2) second form, Σ Λ²/n·(L − log n) = L³/6 + O(L²) | — | **DERIVED** — `integrated_second_moment` (finite Abel; equality grade) |
| Prop 5.6 general-window diagonal, Σ aₙ²·g(log n) | — | **DERIVED** — `tapered_diagonal`; row **closed by F4b**: `cheb2b_of_interface` covers [Z23]'s `ChebyshevMertens.cheb2b` — the only coefficient-reading input of their own kernel-checked sandwich (`sum_a2g_upper'`/`sum_a2g_lower'`) — at its literal statement |
| Lemma 5.2 (Montgomery–Vaughan Hilbert inequality) | none (coefficient-free analytic inequality; already formalized in `Zeta23/MV`) | analytic, not arithmetic |
| (5.3) frequency spacing δₙ⁻¹ ≤ 2n and Σ aₙ²/δₙ ≪ XL | integer support geometry + Σ f² | **DERIVED** (F1.3 spacing; then F1.1) |
| (5.4) pointwise ν_X bound | Σ f/√n + Stirling | **DERIVED** (`sum_div_sqrt_le`) + archimedean |
| Prop 5.3 (trace): µ-part / P-part / Π-part | archimedean / Σ f/√n / pole term | archimedean + **DERIVED** + **PACKAGE** |
| Prop 5.5 (M[µ,µ]) | none (archimedean) | archimedean |
| Prop 5.6 off-diagonal O₁, O₂ | MV + Σ f², (Σ f/√n)² | **DERIVED** |
| Prop 5.7 (cross terms) | Σ f/√n, Σ f/(√n·log n)-type sums | **DERIVED** (`sum_div_sqrt_le`, `sum_mul_log_div_sqrt_le`) |
| §2 explicit formula; App A normalization | functional equation, entire completion, contour | **PACKAGE** |
| §4 zero side: ρ ↦ 1−ρ̄ symmetry; N(t+1)−N(t) ≪ log; RvM; tail Prop 4.2 | zero-multiset properties | **PACKAGE** (and the inertia core is already abstract: `strut_hyperbolic_shadow_bound` holds for *any* configuration) |
| §6 assembly | bookkeeping | — |

## F0 verdict at the time (morning of Aug 12; superseded by F4a's arithmetic completeness)

**The prime side factors.** Every arithmetic input of [Z23] §5 is a field of `EulerStiffness` or derivable from the fields. No hidden coefficient fact was found. Two precise qualifications:

1. **Endpoint grades.** The crude (Cauchy–Schwarz) derivations lose a log factor on the √-moments, which survives every bandwidth λ < 1 and hence yields the full 2/3-liminf constants via λ → 1⁻ and dyadic summation ([Z23] Remark 6.1 makes exactly this move available). The sharp λ = 1 endpoint uses the Abel-summed grades — still interface-derivable, just more Lean work (F2).
2. **The residue is the completed-function package, by design.** The explicit formula for the specific function, the ρ ↦ 1−ρ̄ symmetry, the Riemann–von Mangoldt density, the unit-interval zero count, and Stirling are properties of the completed object, not of coefficients — the interface was never meant to carry them. The abstract factorization theorem therefore has the shape: *completed-package hypotheses* + `EulerStiffness` ⟹ two-thirds-type bound.
3. **A genuine finding — the interface is the ζ-shaped special case.** [Z23]'s own Theorem E (Dirichlet L-functions) reruns the proof with coefficients Λ(n)χ(n), which are **complex**; `EulerStiffness` as defined (real, nonnegative) does not cover them. The prime side there uses modulus controls |f(n)| ≤ log n plus exact diagonal |χ(n)|² = 1. So the general factorization requires the modulus form — **`EulerStiffnessC`** — which F3 subsequently implemented as *exactly* `EulerStiffness (fun n => ‖g n‖)`: no additional field proved necessary (this paragraph's earlier conjecture of a "diagonal-exactness field" was an intermediate diagnosis, superseded by the F3 design — the diagonal exactness is already the modulus second moment). That Theorem E exists and goes through with only these changes is strong evidence the factorization is real — [Z23] already ran the proof through the interface twice, for two different instances, without naming it.

**Answer to the ledger's question, at current evidence:** *it factors, at the architecture level* — with the completed-package residue named (as it must be) and one required generalization (complex/modulus form) identified. What remains to convert this from audited claim to theorem is the Lean work below.

## Canonical status statement (external wording, fixed after review round 3)

> We audited eleven coefficient-dependent touchpoints in [Z23] §5. Each is covered by a machine-checked theorem derived from the five-condition modulus interface. We found no additional coefficient dependence. The remaining unformalized assembly consists of the coefficient-independent analytic scaffolding identified in [Z23] §7.1. We therefore claim **arithmetic completeness of the interface — five modulus conditions suffice for every identified coefficient-dependent input** — not yet a formal abstract version of Theorem 5.8.

Three precision rules bound this claim. **(1) Sufficiency, not necessity**: no minimality claim is made — none of the five conditions has been shown unweakenable. **(2) Enumeration is not kernel-verified**: the kernel certifies that each of the eleven touchpoints is covered; that the *list of eleven is exhaustive* is a human/AI audit of [Z23] §5. **(3) Correspondence is not kernel-verified**: each closure-table row carries an identification claim — that the covering theorem's statement *is* the touchpoint, at the strength [Z23]'s usage instantiates — which the kernel does not certify. Rows split into **statement-identical** coverings (e.g. `integrated_second_moment` ↔ (5.2b): same formula) and **shape-coverings** requiring an identification argument — chiefly `tapered_diagonal` ↔ Prop 5.6/§7.1 (whether [Z23]'s concrete tapered windows satisfy the sandwich hypotheses, in particular the lower tent at M) and the composed rows (ν_X bound; Prop 5.7 cross terms). *(Update: the chief row is now kernel-closed — see F4b — leaving the composed rows as the remaining rule-3 surface.)* External review should spend its minutes on the shape-covering rows; rules (2) and (3) are jointly where an error would live if one lives anywhere. On the counterexample: `dh_not_eulerStiffnessC` is a genuine second check — the 1 + κ² defect survives the modulus embedding — beyond the single support-gate `dh_not_eulerStiffness`.

## Interpretation: the ceiling–factorization reading (audit-level, demoted from "result" on review)

The pre-registered failure point for F4 — an off-diagonal bilinear sum inexpressible from the five diagonal fields — resolved in its contrapositive. The closure table shows Prop 5.6's off-diagonal terms O₁, O₂ covered by the Montgomery–Vaughan inequality (coefficient-free) plus *diagonal-derived* moments: in the bandwidth-≤1 regime, **[Z23]'s argument never evaluates off-diagonal prime correlations — it only dominates them.** This observation is supported by the closure table and is, we believe, the structural reason the factorization succeeded where it was pre-registered to fail.

What is **not** established, and was overstated in an earlier revision of this section: a metatheorem that *no conceivable argument* operating only on the five fields can exceed [Z23]'s ≈ 0.68185 ceiling. The Lean results prove sufficiency of the interface for *this proof architecture*; an information-theoretic ceiling over all proofs factoring through the interface would require its own impossibility theorem (e.g., two admissible interface-indistinguishable models with different zero behavior), which has not been attempted. The honest deliverable is therefore:

> **A precise interface against which future improvements can be dependency-audited: an argument requiring arithmetic information not derivable from `EulerStiffness` has consumed genuinely new prime-side input relative to the [Z23] architecture.**

This is still the program's first *production* in the ledger §5 sense — a usable dependency-audit standard — claimed at exactly the strength the formal record supports.

## Milestones

- **F1 (this commit): the derivable-lemma layer, easy grade.** `Factorization.lean`: F1.1 second-moment bound Σf² ≤ (log x)·(log 4+4)·x; F1.2 crude √-moment (Σ f/√n)² ≤ x·Σ f²/n (Cauchy–Schwarz — pairs with `mertens_energy` for the √x·log x grade); F1.3 the MV frequency spacing log(n+1) − log n ≥ 1/(n+1) from integer support alone. All from the five fields; kernel-checked.
- **F2a (DONE, same day): the sharp √-moment — by dyadic descent, not Abel.** `sum_div_sqrt_le` / `sum_div_sqrt_le_real` (kernel-checked): Σ f(n)/√n ≤ 2(√2+1)(log 4+4)·√x from `chebyshev` + `nonneg` alone. The proof avoids [Z23]'s Abel-summation route entirely: split at n/2, the top block pays (√2/√n)·chebyshev, the bottom recurses; the geometric series telescopes with the exact constant 2(√2+1) = 2/(√2−1). No integrals, no measure theory — finite sums only. **This closes the λ = 1 endpoint**: with F2a, every bound-grade estimate in [Z23] Lemma 5.1 is now interface-derived at full strength. ([Z23]'s constant is 3; ours ≈ 26.2; their Lemma 5.2 remark licenses any absolute constant.)
- **F2b.1 (DONE, same day): the integrated second moment — equality grade, fully discrete.** `integrated_second_moment` (kernel-checked): for any interface-f, Σ_{n≤N} f(n)²/n·(log N − log n) = log³N/6 + O(log²N), **from the `mertens_energy` field alone**. This is the diagnostic result: an inequality can survive abstraction by discarding information, but recovering the exact L³/6 normalization asks whether the interface retains the asymptotic *mass* — and it does. The proof is a **finite Abel summation with no integrals and no measure theory**: a triangular sum swap (`abel_swap`, proved via `Finset.sum_comm'`), telescoping cubes and squares over `Ico` (`telescope_Ico`), per-step comparisons |½a²Δ − (b³−a³)/6| ≤ ½Δ²b and aΔ ≤ ½(b²−a²) (`cube_step`, `sq_step`), and two helper sums — the harmonic bound Σ1/m ≤ 1 + log N, **whose induction step is paid by F1.3's own spacing lemma**, and Σ1/m² ≤ 2 by telescoping. Supporting cast: `apply_one` (f(1) = 0 from the support field), `harmonic_le`, `inv_sq_le`, `log_succ_sub_le` (Δ_m ≤ 1/m). Fifteen theorems total in `Factorization.lean`, all `[propext, Classical.choice, Quot.sound]`.
- **F2b.2: the g-weighted diagonal.** Σ f(n)²/n·g(log n) main term for tapered windows (‖g′‖ ≤ 2) — the same finite-Abel skeleton applies (swap against g-increments instead of log-increments); natural to fold into F4's window machinery rather than build standalone.
- **F3 (DONE, same day): the modulus interface — phase-blind by definition.** Style-law audit mid-build (McSheery: "are we staying in Cl(N,N) and avoiding the complex plane?") caught the first draft carrying ℂ in a structure definition. The compliant design is stronger: since every interface field depends only on ‖g‖, `EulerStiffnessC g` is *defined* as `EulerStiffness (fun n => ‖g n‖)` — the concept is modulus + rotor, the interface consumes the modulus (a real object), ℂ appears only as the standard model (shadow, never carrier). Consequences become near-definitional: `EulerStiffnessC.norm` (modulus reduction) is `id`; **`EulerStiffnessC.twist`** (rotor invariance — the Dirichlet shape, [Z23] §7.3's "phases enter all bounds only through |aₙ|") is a `funext`; F2a/F2b transfer as one-line corollaries (`sum_norm_div_sqrt_le`, `integrated_second_momentC`); ζ embeds (`eulerStiffnessC_vonMangoldt`); and D-H is excluded from the modulus interface verbatim (`dh_not_eulerStiffnessC` — the 1 + κ² defect survives the embedding). Twenty-two `Factorization` theorems, all standard axioms. **F3b-general (DONE)**: the instance problem decomposed and its general half proved. `defect_stability` (kernel-checked): removing boundedly much energy from an interface system — pointwise domination 0 ≤ f′ ≤ f plus removed-energy bound Σ(f² − f′²)/n ≤ D — preserves all five fields (the Mertens constant shifts by ≤ 2D + 1). `masked_twist` (kernel-checked): a selector with values in {0} ∪ {unimodular} — precisely the shape of a Dirichlet character with its conductor zeros — sends an interface system into the modulus interface given the removed-energy bound; the character's phases vanish by F3's phase-blindness, so **only the mask's energy cost survives as a hypothesis**. **F3b-instance (CLOSED — arithmetic core + named corollary below)**: for χ mod q — *any* character: primitivity is not needed for interface membership (‖χ(n)‖ ∈ {0,1} holds for all characters) and belongs to the completed-function package, not the arithmetic side — the bad-prime energy bound Σ_{p ∣ q} Σ_{p^k ≤ x} log²p/p^k ≤ D_q (a q-dependent constant: each prime's tower is a convergent geometric series) — plus the Mathlib `DirichletCharacter` plumbing identifying ‖χ(n)‖ ∈ {0, 1}. With those two pieces, `masked_twist` yields Λχ ∈ 𝓔 — the second genuine inhabitant.
- **F4a (DONE, same day): arithmetic closure of the prime side.** The last two coefficient-consuming touchpoints of [Z23] §5 are now interface-derived: **`tapered_diagonal`** — the arithmetic diagonal input to the general-window Frobenius asymptotic (Prop 5.6 / §7.1; not the asymptotic itself), proved by sandwiching the window between sharp cutoffs at N and M and applying F2b.1 twice, with defect C·log²N + (taper width)·log²N, exactly [Z23]'s (1 + O(w/L)) shape; and `sum_mul_log_div_sqrt_le` (the log-weighted √-moment). **The arithmetic-closure table** — every step of [Z23] §5 that reads coefficients, with its covering theorem:

  | §5 coefficient touchpoint | Covering theorem (interface-derived) |
  |---|---|
  | Σ Λ(n) ≪ x (Lemma 5.1) | `chebyshev` field / `psi_nat_le` |
  | Σ Λ²/n = ½log²x + O(log x) | `mertens_energy` field |
  | Σ Λ(n)² ≪ x log x | F1.1 `sum_sq_le` |
  | Σ Λ/√n ≪ √x (sharp, λ=1) | F2a `sum_div_sqrt_le` |
  | Σ Λ·log n/√n | F4a `sum_mul_log_div_sqrt_le` |
  | MV frequency spacing; Σ aₙ²/δₙ | F1.3 `freq_spacing` + F1.1 |
  | integrated second moment (5.2b) | F2b.1 `integrated_second_moment` |
  | tapered/general-window diagonal (Prop 5.6, §7.1) | F4a `tapered_diagonal` |
  | O₂ off-diagonal ((Σ aₙ)²) | F2a squared |
  | Prop 5.7 cross terms (Σ aₙ/log n-type) | F2a (log n ≥ log 2 on the range) |
  | complex/Dirichlet coefficients (Thm E) | F3 modulus interface + `twist` |

  **No obstruction was found: every *identified* arithmetic touchpoint sees the interface, not Λ** (enumeration caveat in the canonical status statement above). The remaining unformalized §5 content — the Gabor/Poisson window identities, Dirichlet-kernel bounds, Stirling, and the assembly of Theorem 5.8 — is *coefficient-free by [Z23]'s own accounting* (§7.1: "Nothing in Sections 4–5 used that φ is flat-topped, only [window properties]"; the zero side is already abstract via `strut_hyperbolic_shadow_bound`). Precise status: **the factorization holds at arithmetic completeness** — the abstraction demonstrably supplies every coefficient input at full strength, bounds and main terms both. This is strictly stronger than F0's "architecture level" and strictly weaker than the full abstract Theorem 5.8, which additionally requires formalizing the coefficient-free analytic scaffolding (**F4-final**, large, zero arithmetic content, hence no *interface* obstruction can arise there on [Z23]'s own accounting — a research assessment, stated here and not for external use until F4-final compiles).
- **F5 (conditional on F4): the abstract theorem.** Completed-package + `EulerStiffnessC` ⟹ 2/3-type bounds; [Z23] Theorems A–E become two instantiations.

## Registered constraints and open problems (round 5)

**F3b-instance constraint (registered per review): the interfaces are frozen for this test.** If the Dirichlet instance cannot be established from `defect_stability` + `masked_twist` + the bad-prime energy bound + Mathlib character facts *without modifying* `EulerStiffness`/`EulerStiffnessC`, the correct move is to stop and classify the missing assumption — the classification is the result. **The arithmetic core is now complete (same day):** `geom_tail_le` (per-prime geometric tail, via the tower telescope imported from SpongeStage) → `prime_tower_energy` (each prime's stratum carries energy ≤ log²p/(p−1): the p-power support reindexes along the valuation to a geometric tail) → `bad_prime_energy` (the union bound over a finite prime set, by cons-induction with `sum_union_inter`) → **`euler_factor_deletion`**: ζ's von Mangoldt system, masked at finitely many primes and twisted by *any* selector with values in {0} ∪ {unimodular} whose zeros lie over the deleted primes, inhabits the modulus interface, with defect constant D_Q = Σ_{p∈Q} log²p/(p−1). **Interfaces frozen throughout — the registered constraint held: nothing in `EulerStiffness`/`EulerStiffnessC` was modified.** This is the Dirichlet instance in abstract-selector form; the named `DirichletCharacter` corollary is now pure Mathlib plumbing (‖χ(n)‖ ∈ {0,1} for character values; zeros exactly over p ∣ q via `ZMod.isUnit_iff_coprime`) on top of a finished theorem. The working title's "stable under finite Euler-factor deletion and unimodular twisting" clause is hereby earned at the abstract level. **The named corollary is now kernel-checked (same day): `dirichlet_inhabitant`** — for every `q ≠ 0` and every `χ : DirichletCharacter ℂ q`, `EulerStiffnessC (χ(n)·Λ(n))`, by `euler_factor_deletion` with `Q = q.primeFactors`; the plumbing is exactly as predicted (`DirichletCharacter.unit_norm_eq_one` / `MulChar.map_nonunit` for ‖χ(n)‖ ∈ {0,1}, `ZMod.isUnit_iff_coprime` + `Nat.exists_prime_and_dvd` for zeros-over-p ∣ q; primitivity not needed, per the registered note). Standard axioms only; interfaces frozen throughout. **F3b-instance is CLOSED. The second inhabitant is named.** Audit: 85 theorems, 85 audited, 0 gaps, 0 sorryAx.

**F6 (registered open problem, reformulated on review): the ceiling metatheorem — over the F5 package.** Construct two systems satisfying the same `EulerStiffnessC` arithmetic interface **and the requisite completed-package hypotheses**, but having different critical-line proportions; or prove that the completed-package hypotheses prohibit such a pair. (The earlier formulation quantified over bare coefficient systems, presupposing a coefficient-to-completed-object realization the interface deliberately does not contain.) This is the sharper question in any case: it asks exactly where rigidity lives — arithmetic interface vs completed-function package. Registered while the answer is unknown.

**Adapter theorems (F4-final component).** Each shape-covering row of the closure table (precision rule 3) should eventually become a kernel-checked adapter: `Z23WindowHypotheses(g,N,M) ⟹ TaperedDiagonalHypotheses(w,N,M)`. Every correspondence claim moved into the kernel shrinks the remaining epistemic surface — which quietly re-types F4-final itself: from a single distant "formalize the scaffolding" milestone into a program that *monotonically shrinks the audited surface*, one adapter at a time, with the precision-rule machinery automatically recording each conversion. The claims can only move toward the kernel. Priority adapter: the Prop 5.6/tapered-window row.

**F4b (DONE, 2026-08-12): the priority adapter, closed at a sharper seam than registered.** Reading [Z23]'s own formalization (`Zeta23/PrimeSideB/PP.lean`) showed the planned `Z23WindowHypotheses ⟹ TaperedDiagonalHypotheses` adapter was the wrong factoring: their kernel-checked sandwich proofs (`sum_a2g_upper'`/`sum_a2g_lower'`) already reduce the concrete windows to tent facts ([eq:gbounds], coefficient-free) plus **exactly one arithmetic input** — `ChebyshevMertens.cheb2b`, the real-cutoff integrated second moment. The whole of [Z23] §5 reads coefficients only through the six fields of `Zeta23.ChebyshevMertens` (`Zeta23/Hypotheses.lean`). The adapter therefore lands at their consuming type: **`cheb2b_of_interface`** (floor transfer over `integrated_second_moment`; the `log x − log⌊x⌋ ≤ log 2` gap costs one `mertens_energy` evaluation plus a cube difference), **`cheb1c_of_interface`** (log-damped √-moment by split at √x; `√(√x)·log x ≤ 4√x` absorbs the log), and **`chebyshevMertens_of_interface`**, which assembles [Z23]'s literal `ChebyshevMertens` structure from the interface — **five of six fields interface-derived** (`cheb1a` = the `chebyshev` field verbatim; `cheb1d` = F1.1; `cheb2a` = the `mertens_energy` field verbatim; `cheb1c`, `cheb2b` = the new theorems). **The residue is named, not hidden: the single field `cheb1b`** (Σ Λ(n)/√n ≤ 3√x eventually) hardcodes a constant sharper than the interface's `chebyshev` field carries — the interface grade-covers it at `2(√2+1)(log 4 + 4)` (`sum_div_sqrt_le_real`) but cannot reach the literal 3, which classically needs ψ(x) ≤ c·x with c ≤ 3/2 (Chebyshev's log 4 suffices; the interface field's log 4 + 4 does not). It enters the assembly as an explicit hypothesis, discharged by [Z23]'s own `cheb1b` when instantiating. No modification to either side's statements; interfaces frozen throughout. **Consequence for the precision rules: the chief shape-covering row of rule 3 is closed.** The remaining rule-3 surface is the composed rows (ν_X bound; Prop 5.7 cross terms) and rule 2 (exhaustiveness of the touchpoint list). A parallel observation for later: [Z23]'s Theorem E consumes `ChebyshevMertensCoprime q` (`Zeta23/ThmE/Hypotheses.lean`) — the Dirichlet-side analogue of this seam, adjacent to `dirichlet_inhabitant`.

**Working title for the eventual external note** (review-proposed; the F3b-instance condition is now met — `dirichlet_inhabitant` compiles — so every clause of the title is earned; the title **remains provisional** until the note itself is drafted and externally reviewed): *A machine-checked arithmetic factorization of the [Z23] prime side through a five-condition modulus interface, stable under finite Euler-factor deletion and unimodular twisting.*

**Freeze clarification (registered before the instance build): modification vs parameterization.** The frozen-interface rule forbids changing the *fields* of `EulerStiffness`/`EulerStiffnessC`. It does **not** forbid instance-level parameters: the defect constant D_q = Σ_{p∣q} log²p/(p−1) is q-dependent and that is fine — the inhabitant claim is per-character (for each fixed χ mod q, Λχ satisfies the interface); no uniformity in q is claimed or needed for membership, and "second inhabitant" must not be read as a uniform-family claim.

## Registered strategy fork (2026-08-12, post-F3b/F4b, per convergent external reviews)

Four independent reviews converged on the same re-typing: the factorization is no longer the destination; it is the platform. The certification brake stays on all claims (every demotion in this record raised its value); the change is in targets. Two tracks, run in parallel:

**Track A — the publishable certificate (short horizon).** (i) The one-page external note (working title above; no ledger/sponge/Clifford vocabulary; canonical status statement; three precision rules; F4b's named residue is part of the story, not a blemish). (ii) Contact while first-downstream-dependency status still holds: an issue/discussion post on `anthropics/zeta-23-lean` describing the bridge pin and the audit; Mathlib-PR candidates (`geom_tail_le`, `telescope_Ico`, `harmonic_le`, the Ioc split kit); one named human formalizer recruited to audit any single file (closes the all-AI-review provenance gap the ledger discloses). (iii) Zenodo v2 after the note.

**Track B — the research program (standing).** Registered experiments, in review-converged priority order:
- **F7 (beyond-interface / information ladder, E₀ → E₁):** starting from the first place Fourier support > 1 introduces genuinely off-diagonal arithmetic, identify the minimal additional coefficient invariant (a correlation field) needed to reproduce that step; formalize `EulerStiffness⁺ = EulerStiffness + one correlation field`; determine what stronger zero-side conclusion the augmented interface supports. Prize: a formal dependency ladder E₀ ⊂ E₁ ⊂ ⋯ ("arithmetic information level ↔ allowed signature defect on the zero side"). The F4b residue is the first datum: `cheb1b`'s constant already sits *between* E₀ and PNT-strength input.
  **Design sharpening (registered 2026-08-12, per review):** E₁ is to be *derived, not appended*. The two-point observable C₂(m,n) = aₘ·conj(aₙ) has E₀ as exactly its diagonal (|aₙ|² — what `EulerStiffnessC` sees, being defined through the modulus); E₁ is the controlled off-diagonal part, entering through `PairEnergy_K(a) = Σ_{m≠n} aₘ·conj(aₙ)·K(log m − log n)` for explicit-formula-shaped kernels K. Work plan: new file `InformationDepth.lean`; freeze `EulerStiffness` as E₀; do **not** pre-commit E₁'s fields — take the first [Z23]-adjacent calculation beyond the diagonal regime and let the minimal invariants of `PairEnergy` needed to close it *define* E₁. First falsifiable target: a **counterfeit pair** — two coefficient systems indistinguishable to E₀ (both inhabit; note D-H does *not* qualify, it is rejected at E₀ — the search is for systems that pass) but distinguishable by the first off-diagonal observable. That pair fixes E₁'s content empirically. **Target won (same day, `InformationDepth.lean`): `counterfeit_pair` is kernel-checked** — Λ and its sign-flip at n = 2 have pointwise equal modulus (hence both inhabit, and are indistinguishable to *any* modulus functional — `eulerStiffnessC_of_modulus_eq` states E₀'s modulus-blindness in that strong form), yet `PairEnergy` at constant kernel, N = 3, separates them exactly: 2·log 2·log 3 vs −2·log 2·log 3. Supporting structure: `C2_diag` (the diagonal of the two-point observable *is* E₀'s data) and `offdiag_split` (full two-point sum = E₀-visible diagonal·K(0) + `PairEnergy`). Seven theorems, standard axioms. Registered limits hold: E₁ is *not* yet defined — the flip pair certifies the informational gap and the direction (off-diagonal restoration); E₁'s fields await the first [Z23]-adjacent beyond-diagonal calculation. No zero-side claim is made.
  **Experiment 1 (same day, oracle + report at `experiments/EXPERIMENT_1_InformationDepth.md`, independently re-run and verified):** five systems on the exact E₀-fiber of Λ (verified field-identical to 9 decimals; the χ₄ near-fiber control reproduces the F3b defect D₄ − tail *uninvited*, a cross-experiment consistency check). Three structural findings, claim-typed: **(i) strict hierarchy witnessed** — the per-prime random-rotor pair is tower-identical (machine-zero at all bandwidths; algebraic half now kernel-checked as `rotor_pair_invariant`) but cross-separated at full amplitude: E₀ ⊊ E_tower ⊊ E_cross with explicit witnesses. **(ii) The stealth counterfeit** — the design prediction that the parity pair (λΛ, the classically dangerous twin: its completed object has poles at ζ's zeros) separates at tower level was **falsified**: λ(p)λ(q) = +1 makes it *exactly* invisible on prime-prime pairs (kernel-checked: `liouville_pair_invisible`), which dominate pair energy; its entire two-point fingerprint lives on the thin mixed-grade locus p^j ≈ q^k, j+k odd. Interpretation (typed conjecture, elevation-checked): the fiber carries an observability stratification in which the dangerous direction hides best — a computable articulation of why parity is the barrier. **(iii) Depth is observable-relative** — the one-point signed sum separates the parity pair at first order (−ψ + 2ψ_□ checks exactly) while two-point energy nearly misses it; [Z23]'s own structure aligns: its *bounds* consume the fiber-collapsed moduli (E₀), its *main terms* are one-point phase data from ζ itself. Consequence for the E₁ design: "depth" is not a single ladder — the observable family must be part of E₁'s definition. `tower_horizon` (visibility switches on at Δ = log p) is also kernel-checked. **Target 4 CLOSED (same day): `parity_diff_support`** — the exact support theorem: PairEnergy(λΛ) − PairEnergy(Λ) = Σ over {m≠n : Ω(m)+Ω(n) odd} of −2·Λ(m)Λ(n)·K(log m − log n), for every kernel and truncation. Every even-grade pair (in particular every prime-prime pair, the dominant stratum) contributes exactly zero to the difference; the stealth-counterfeit finding is now a kernel-checked identity, the first theorem of the observability stratification. Statement cross-checked numerically (full difference = mixed-grade sum to machine precision, N = 2·10⁴). **Refined F6 (registered):** can package-preserving twists (characters) be separated from package-breaking twists (λ, random ε) by any computable functional of coefficients alone, or is package-membership itself deep? The oracle is the instrument. Depth vocabulary (audit-level until theorems exist): d(T) = min k such that E_k ⊢ T; the arithmetic side of [Z23] is d = 0 by the closure table; candidate readings — bandwidth ≤ 1 as the resolution horizon below which rotor-relative information is invisible — are interpretation, subject to the five checks, and flattering ones get checked hardest.
- **F8 (arithmetic curvature — registered 2026-08-12, adopted from external review as the one grand-program item with an existing kernel foothold):** generalize the D-H discriminator cell into a tensor. Define the multiplicativity-defect D(m,n) = c(mn) − c(m)c(n) on the log-derivative coefficients — the n = 6 cell *is* our existing `logDeriv6_eq_defect` machinery, where D-H's D(2,3) = (1+κ²)·log-normalized ≠ 0 is already kernel-checked in essence (`dh_logDeriv6_pos`) — and the curvature energy 𝒦(a) = Σ w_{m,n}|D(m,n)|². Euler products are 𝒦-flat by definition of complete multiplicativity on the relevant coordinates; D-H is curved at its first mixed-prime cell. First targets: (a) formalize D as a function and prove the ζ-flatness cells + the D-H curved cell as instances of one definition (near-free from existing theorems); (b) ask the quantitative question — do `EulerStiffness` inhabitants occupy a low-𝒦 stratum? — as an oracle experiment before any formalization. The audacious reading (off-critical zeros as curvature-induced hyperbolic modes) is horizon vocabulary, unclaimed. Adjacent registered vocabulary from the same review, audit-level only until a theorem exists: renormalization reading (dyadic descent = coarse-graining step, `defect_stability` = irrelevant perturbation, `masked_twist` = gauge transformation + local mask — note `rotor_pair_invariant` is already the first gauge-invariant fact, so the "gauge quotient" program is InformationDepth by another name); defect–information inequality H(X) ≤ Φ(U_k(X)) (no toy model exists yet; the function-field calibration already registered is its natural first laboratory).
- **F6 via Beurling generalized primes, with field attribution:** for each interface field, either the Beurling toolkit (Diamond; Zhang; Broucke–Vindas) constructs a system satisfying it with wild zero behavior, or the field resists — the resistance pattern is a rigidity-attribution result regardless of which way the metatheorem falls.
- **Minimality of the five fields:** for each field, derive-or-countermodel from the other four (the canonical statement claims sufficiency only; make necessity a problem, not a caveat).
- **Inhabitant classification / closure calculus:** which standard L-function operations (finite Euler-factor modification, twist, product/convolution, induction) preserve `EulerStiffnessC` membership; `ChebyshevMertensCoprime q` is the natural next consuming type (adjacent to `dirichlet_inhabitant`).
- **Prospective GL(2) shopping list:** run the closure audit *forward* — what would instantiating the abstract shape for a holomorphic cusp form L-function require; which package facts are known, where it breaks. The certificate machine pointed at a proof nobody has finished yet.

The frame (split signature, modulus/rotor, deletion, collapse) remains the private discovery heuristic — export only the conventional mathematics it predicts; `rotor_telescope` appearing in this file's import graph is the model for how it earns public lines. D-H falsification discipline applies unchanged to every Track B extension: `EulerStiffness⁺` must still reject D-H at E₀ and must *state* what D-H inhabits at which level, if any.

## Development artifacts (engineering register, not evidence)

`verify_f3b.py` (repo root of this subproject) is the F3b numerical oracle: 12 moduli to x = 10⁶ including the primorial 9699690, all PASS, with the *exact* margin identity D_q − M_q(x) = Σ_{p|q} Σ_{p^k>x} log²p/p^k confirmed to 1e-9 — stronger than the formalized inequality (`bad_prime_energy` drops the nonneg tails; the identity form is a registered optional strengthening). Register: computed-in-code; it is a regression test and statement-shape check, **not** mathematical evidence, and is never to be cited as "numerical verification of F3b." The parallel-lane draft it accompanied (`F3bInstance.lean`, 8 sorries, never compiled) is archived with a supersession map at `archive/f3b_handoff_2026-08-12/` — kept because the pre-registered route (per-character scope, no primitivity, NeZero threading, worst constant p = 2) matches the completed build exactly, which is itself a data point for the discipline.

## Falsification discipline

Per the standing rule: the interface must continue to *reject* Davenport–Heilbronn. The kernel-checked record is a pair: `dh_not_eulerStiffness` (the support gate at n = 6: the defect (c(6) − c(2)c(3))·log 6 = (1+κ²)·log 6 is a sum of squares, nonzero for every real κ) and `dh_not_eulerStiffnessC` (a genuine second check: the defect survives the modulus embedding). Any abstract theorem in F4/F5 that would apply to D-H is wrong by construction and must be discarded.
